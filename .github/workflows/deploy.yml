name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  quality:
    uses: ./.github/workflows/code-quality.yml

  deploy:
    needs: quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Install Dependencies
        working-directory: frontend
        run: pnpm install

      - name: Build
        working-directory: frontend
        env:
          NODE_ENV: production
          NEXT_PUBLIC_BASE_PATH: /The-Good-AI-Lab
          NEXT_PUBLIC_ASSET_PREFIX: /The-Good-AI-Lab
        run: |
          pnpm next build && touch out/.nojekyll

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: frontend/out
          branch: gh-pages
          clean: true
